# CURRENT PROJECT PHASE

## Project
Summit Construction Technology & Restoration Group — Summit HVAC platform

## Current Phase
Phase number: 5 of 6 (of the completion plan started 2026-08-22)
Phase name: Testing & Deployment Readiness

## Phase Objective
Add a real test runner, add direct unit tests for the calc engines (previously zero direct coverage beyond report-layer regression tests), fix Puppeteer for actual Vercel deployment, add a minimal CI workflow.

## Status
COMPLETE — all 4 phase items done, verified, and on `origin/main`. **This session (2026-08-23, second entry)**: resolved the open questions carried over from Phase 5 closeout — see "Open Questions Resolved This Session" below — before starting Phase 6 scoping. **Ninth entry, same day**: the user re-pasted the original "master project recovery" prompt as a check-in ("give an update... continue where you left off"), not a request to redo it — its Phase-0 goals (export cleanup, GitHub verification, memory-system structure) were already resolved in this same session's second entry, and this repo's own `CLAUDE.md` already codifies the decision to keep this 3-file memory system over the `docs/` tree that prompt describes. Answered with a status update in the format the prompt itself specifies, then continued the actual engineering work already in progress (see entries five through nine below).

**Eleventh entry, same day — TWO CORRECTIONS the user had to make, both now fixed in the record below and in Claude's persistent memory:**
1. **Vivian Street was never intended to have drawings, at all, ever** — it's a sample/reference report defining what Summit needed to produce, not a project with a "missing" plan set. Every mention below of "sourcing the real dimensioned plan set for Vivian Street" (third, fourth, and fifth entries) was wrong and should not be acted on — there is no plan set to find, by design.
2. **Kinsela's "missing" room data was misdiagnosed as needing field measurement** (fifth/sixth/seventh/ninth entries below) — the user correctly pointed out the real drawing has real printed dimensions. Investigation confirmed: (a) at least 2 rooms (Laundry, Master Bath) have real dimensions printed directly on the sheet that extraction simply failed to read — a genuine model-reading miss, not missing field data; (b) a real, confirmed, now-fixed code bug in `applyExtractedData`'s re-extraction reconciliation was silently dropping genuinely new rooms the drawing showed but that didn't match an existing room by name — recovered 5 real rooms live on Kinsela as a result. See the eleventh-entry bullet under Systems / Features Implemented for the full fix.

**Twelfth entry, same day — a THIRD correction, on the same theme**: told to actually read the drawing "as an architect" and stop treating unresolved geometry/compass-mapping as unreachable. Rendered the real drawing pages as images (`pymupdf`/`fitz`) and read 6 rooms' printed dimensions directly, then derived the page-to-compass wall-length transform from two facts already on the drawing (FRONT PORCH/REAR PORCH page position + the project's confirmed `building_front_faces`) rather than guessing — validated the derivation against a room the pipeline had already gotten right before trusting it. See the twelfth-entry bullet under Systems / Features Implemented for the full result, including two transcription corrections to earlier reads and 3 confirmed-duplicate rooms still needing a user decision to delete.

**Thirteenth entry, same day — the user escalated from "fix Kinsela" to a standing product requirement**: "SUMMIT MUST BE ABLE TO READ ALL PAGES AS A MASTER LEVEL ARCHITECT ON EVERY PROJECT EVER CREATED IN SUMMIT AND BE ABLE TO CORRECTLY READ EVERY PAGE TO GET THE INFO REQUIRED CORRECTLY." This is not a request to hand-fix more projects one at a time — the twelfth entry's manual work doesn't scale to "every project ever created." Built a real, systemic fix to the actual extraction pipeline instead: `lib/pdfTextExtraction.ts` (new), a deterministic PDF-text-layer read (`pdf-parse` v2, chosen over the older v1.1.x line after v1.1.4's bundled pdfjs-dist was directly confirmed to throw "bad XRef entry" on a spec-valid PDF — a real robustness gap for a fix meant to run on every drawing, not just the ones tested so far) that hands the model the exact, character-accurate embedded text of every PDF page as grounding alongside the visual document, on every extraction call. Live-validated against Kinsela's real drawing: this single change puts BOTH of this session's two previously-diagnosed, documented misses (Laundry/Master Bath's real dimensions; the "R-38 BLOWN INSULATION" callout that even a narrow, directly-targeted follow-up re-ask couldn't recover, per `roomsNeedingCeilingHeightFollowUp`'s own 2026-08-14 diagnosis comment) directly in the model's context on its very next real run. Also added an explicit cross-sheet room-lookup instruction to the main extraction prompt (`buildExtractionPrompt`), encoding the exact reasoning that resolved Kinsela's Master Closet/Master Closet (Second Level) duplicate by hand in the twelfth entry, so the model does that check itself going forward. See the thirteenth-entry bullet under Systems / Features Implemented for the full build and verification detail.

## Overall V1 Completion
Estimated percentage: ~75%
Confidence: Medium-High — calc engines (Manual J/D/S/N/Industrial) and drawing extraction are mature; report generation is substantially complete (Phase 4); testing/CI infrastructure is now real (Phase 5); Vercel deployment itself is still unexercised (see What Is Not Verified); role-based UI completeness beyond Field Tech and Phase 6 (knowledge-base docs) are not yet done.

## What Was Accomplished

**Repo-recovery audit (start of this session):** the user pasted a large generic "master project recovery" prompt whose premises (an unrecovered Claude.ai export, ministry-content contamination, a blueprint-takeoff/estimating/multi-agent product) were independently verified against this specific repo before acting on any of it, per the user's own "never report completed without evidence" instruction. Findings: the export (`CLAUD-HISTORY-COMPLETE-RECORD/`) was already correctly gitignored/uncommitted; zero contamination existed in the tracked repo; the user then chose to keep the existing CLAUDE.md/PHASE.md/SESSION-PROGRESS.md protocol rather than adopt a new `docs/` tree, and confirmed multi-agent architecture is a real (future, not current) intended direction. See `SESSION-PROGRESS.md`'s 2026-08-23 entry for the full mined findings (business model, competitive positioning, related-but-separate businesses, a missing vision doc, one live product-decision conflict, and two previously-uncertain features confirmed already built).

**Phase 5 (this phase, 4/4 items):**
- Added Vitest (`vitest.config.mts`); migrated all three existing hand-rolled `.test.mts` files (`reportValidation`, `reportData`, `manualJ`) from a custom `check()`/`pass`/`fail` harness run via bare `npx tsx` to real `describe`/`it`/`expect`. `npm test` now runs `vitest run` (~0.7s vs. ~23s before).
- Added `lib/__tests__/manualD.test.mts` (16 tests: required-CFM, equal-friction zone friction rates, round/rectangular duct sizing incl. Huebscher equivalent-diameter cross-check, velocity warnings, duct-insulation code-minimum compliance) and `lib/__tests__/manualS.test.mts` (20 tests: bilinear cooling-capacity interpolation incl. clamping, 1-D heating interpolation, balance-point crossing, supplemental-heat gap, ACCA sizing-window gating per equipment type, compatibility scoring incl. weight-dropping for cooling-only equipment, full `evaluateEquipment`/`rankEquipment` integration incl. preference tie-break). 51/51 tests passing total.
- Fixed Puppeteer for Vercel: added `lib/browser.ts`, a shared `launchBrowser()` that uses `puppeteer-core` + `@sparticuz/chromium` when `VERCEL`/`AWS_LAMBDA_FUNCTION_NAME` is set, and the full `puppeteer` package (now dev-only) otherwise. Both call sites (`app/api/reports/route.ts`, `lib/floorPlanRender.ts`) updated to use it — this was a real, previously-duplicated gap (two direct `puppeteer.launch()` call sites, not one).
- Added `.github/workflows/ci.yml` (lint + typecheck + test + build on PR/push to `main`, Node 22, build-time env placeholders matching the three vars the code actually reads).

Incidental fix: a stale duplicate `.next/types/*` build artifact (e.g. `routes.d 2.ts`) was breaking `tsc --noEmit` in isolation; cleared via `rm -rf .next && npx next typegen`. Not a code defect — a leftover from a prior interrupted build on this same disk-space-constrained machine.

## Open Questions Resolved This Session (2026-08-23)

The user made four calls on Phase 5's carried-over open questions before Phase 6 scoping began:

1. **Vivian Street OEM data sourcing → authorized autonomous web sourcing.** Turned out not to need fresh sourcing: the real, cited Carrier 26TPA824W003 extended-performance data (30 points, citation to Carrier's official Performance 18 Product Data PDF) was already sourced and committed in `supabase/migrations/20260811031915_replace_discontinued_equipment.sql` back in Phase 3-era work. Live-DB inspection (via a scratch `@supabase/supabase-js` script, run and deleted this session — not committed) found the **live database had been corrupted on 2026-08-17**: 16 synthetic placeholder performance points had been inserted alongside the real 30 (some overlapping the same outdoor-temp/wetbulb coordinates with different, made-up capacity values — an interpolation-integrity bug, not just a labeling issue), and `equipment_catalog.source_document` for that row had been overwritten to placeholder text. **Fixed live**: deleted the 16 placeholder rows (matched by their exact shared `updated_at` batch timestamp), restored the real citation string to `source_document`. Verified after: exactly 30 performance points remain, all from the 2026-08-12 real-data insert; catalog row's `source_document` now matches the migration file again.
   - Separately confirmed the FIXTURE-PLACEHOLDER / VIVIAN-ST-WHOLE-HOUSE-TEST-UNIT catalog entry described in the 2026-08-17 `SESSION-PROGRESS.md` entry **no longer exists live** — consistent with the per-zone equipment-selection migration (`20260822210000_per_zone_equipment_selection.sql`) having since resolved the whole-house-vs-per-system sizing gap that necessitated it. Vivian Street is confirmed live as a genuine 2-zone project (AHU-1, AHU-2 per `zones` table), both currently unselected (`selected_equipment_id: null`) — selecting real equipment per zone is follow-up report-generation work, not done this session.
2. **AED solar-position approach → source real solar irradiance data** (NOAA/NREL) instead of a from-scratch astronomical engine, when AED is picked back up. Decision recorded only — AED itself is still deferred, no implementation this session.
3. **PWA/offline field capture → confirmed still wanted**, scheduled as a future phase (after Phase 6, exact slot TBD — see Deferred Work).
4. **`summit-platform-vision-AEC-master.md` → written for real** at `REFERENCE-DOCS/summit-platform-vision-AEC-master.md`, reconstructed from the export-mining findings (business model, competitive positioning, draft pricing, the AEC discipline list, and explicit scope boundaries). Marked as a directional vision doc, not a committed roadmap — current build scope is unchanged (HVAC only).

## Files Created
- `vitest.config.mts`
- `lib/__tests__/manualD.test.mts`, `lib/__tests__/manualS.test.mts`
- `lib/browser.ts`
- `.github/workflows/ci.yml`

## Files Modified
`package.json` (test script → `vitest run`; `puppeteer` moved dependencies → devDependencies; `puppeteer-core`, `@sparticuz/chromium`, `vitest` added), `package-lock.json`, `lib/__tests__/manualJ.test.mts`, `lib/__tests__/reportData.test.mts`, `lib/__tests__/reportValidation.test.mts` (Vitest migration), `app/api/reports/route.ts`, `lib/floorPlanRender.ts` (shared launcher), `CLAUDE.md` (one-line SaaS/competitive-positioning clarification), `SESSION-PROGRESS.md` (export-mining findings; this session's open-questions-resolved entry).

**This session (2026-08-23, second entry) additionally created:** `REFERENCE-DOCS/summit-platform-vision-AEC-master.md` (new file, see Open Questions Resolved above). The OEM data fix was a live-database write (Supabase `equipment_catalog`/`equipment_performance_points` tables), not a migration file change, because it was correcting live data back to what the already-committed migration specifies; no schema or migration changed.

**This session (2026-08-23, sixth entry) additionally created:** `lib/dataCompleteness.ts` (`checkDataCompleteness` — see Deferred Work's "sixth entry" bullet below for the full finding this came from) + `lib/__tests__/dataCompleteness.test.mts` (7 tests). Real code, not just docs — `npx tsc --noEmit`, `npm run lint`, and `npm test` (58/58 passing including the 7 new tests) all re-run and passing after this addition. **Deliberately not wired into any report-generation gate, `reportGate.ts`, or UI** — that's a product decision flagged for the user, not made unilaterally; see Open Questions.

**This session (2026-08-23, eighth entry) additionally created:** `lib/__tests__/drawingExtraction.test.mts` (5 tests). **Additionally modified:** `lib/drawingExtraction.ts` (new `windows: ExtractedField<boolean>` field on `ExtractedRoom`, JSON schema template, STEP 4 prompt text, `collectUnresolvedItems`), `components/drawings-section.tsx` (new windows status line + `FieldResolutionBadge`). User-authorized, real production code — see Systems / Features Implemented for the full change and its live validation result.

**This session (2026-08-23, ninth entry) additionally modified:** `lib/drawingExtraction.ts` (`collectUnresolvedItems` extended with `floor_area`/`walls` items — no type change), `components/drawings-section.tsx` (floor area/walls status lines + badges), `lib/__tests__/drawingExtraction.test.mts` (+9 tests, 14 total). No new files. See Systems / Features Implemented for the full change and its live validation result.

**This session (2026-08-23, tenth entry) additionally created:** `lib/__tests__/reportGate.test.mts` (4 tests). **Additionally modified:** `lib/reportGate.ts` (new gate condition + `data_incomplete` blocker code), `components/report-generation-gate.tsx` (label map entry), `REFERENCE-DOCS/SUMMIT-REPORT-STANDARD.md` (§3 fifth bullet). User-authorized hard block, live-validated against real project data — see Systems / Features Implemented.

**This session (2026-08-23, eleventh entry) additionally modified:** `components/manual-j-workflow.tsx` (`buildRoomInsertPayload` shared helper, reconciliation loop restructured to create genuinely new rooms instead of silently dropping them). No new files — no test infrastructure exists yet for this client-only component. Live-validated with real writes to Kinsela's live `rooms` table (5 rooms recovered) via a scratch script, not committed. See Systems / Features Implemented for the full fix and its live result.

**This session (2026-08-23, twelfth entry):** no source files changed — live-database-only writes (Kinsela's `rooms` table: `floor_area_sqft` + all four `wall_*_len_ft` fields on 6 rooms) via a scratch UPDATE-only script, not committed. See Systems / Features Implemented for the full detail.

**This session (2026-08-23, thirteenth entry) additionally created:** `lib/pdfTextExtraction.ts`, `lib/__tests__/pdfTextExtraction.test.mts` (8 tests). **Additionally modified:** `app/api/drawings/extract/route.ts` (deterministic PDF text extracted once per PDF upload, included on both the main and follow-up extraction calls), `lib/drawingExtraction.ts` (new cross-sheet room-lookup paragraph in `buildExtractionPrompt`), `package.json`/`package-lock.json` (`pdf-parse@2.4.5` added as a real dependency; `@types/pdf-parse` briefly added then removed again once v2's own bundled TS types made it redundant). User-directed real product fix (not per-project data entry) — see Systems / Features Implemented for the full build, the dependency-version investigation, and live validation against Kinsela's real drawing.

## Files Removed
None.

## Systems / Features Implemented
- Real test runner (Vitest) wired to `npm test`.
- Direct unit coverage for all three residential calc engines (Manual J already had it from Phase 4; D and S are new this phase).
- Vercel-compatible headless-Chromium launcher, shared between both PDF/PNG rendering call sites.
- CI (GitHub Actions): lint + typecheck + test + build gate on every PR/push to main.
- **(Sixth entry, 2026-08-23) `lib/dataCompleteness.ts`** — a new, additive, read-only diagnostic (`checkDataCompleteness`) that flags rooms with missing floor area or wall geometry, plus a whole-house check for total glazing area being exactly zero. Not wired into any gate yet — see Deferred Work.
- **(Eighth entry, 2026-08-23) Per-room window detection, its own resolvable field** — `windows: ExtractedField<boolean>` added to `ExtractedRoom` (`lib/drawingExtraction.ts`), mirroring the existing `duct_location`/`duct_insulation_r_value` pattern: `true` = a window opening is visible on some side (area may still need sizing separately), `false` = positively confirmed no windows on any side (a real "confirmed windowless" signal that did not exist before this fix), `null` = couldn't tell. `buildExtractionPrompt`'s STEP 4 now asks for this explicitly; `collectUnresolvedItems` emits it as its own `room[N].windows` item (was previously buried inside the room's single bundled prose `reason`, resolved by one Accept/Override that never touched the real geometry fields); `components/drawings-section.tsx` renders a dedicated status line + `FieldResolutionBadge` for it. **Validated with a real, live re-extraction** against the Vivian Street floor-plan crop already in storage: went from 30 undifferentiated bundled flags to 13 rooms cleanly resolved (zero human work) + 17 precisely and individually flagged.
- **(Ninth entry, 2026-08-23) Same fix extended to floor area and wall lengths — lower risk, no type/prompt/apply-logic change.** Discovered while scoping this that `applyExtractedData` has a second path beyond room creation — a re-extraction reconciliation path (~line 780-889) with a genuine safety check (`floorAreaMismatch`) that refuses to apply new wall/window data when re-extracted floor area disagrees with an existing room's value by more than a tolerance. Wrapping `floor_area_sqft`/wall lengths in `ExtractedField<T>` like `windows` would have touched that safety-critical arithmetic across multiple call sites — real risk, unlike windows' purely-additive change. Used a lighter fix instead: floor area/walls don't need a 3-state signal like windows did (it's just "do we have a number or not"), so `collectUnresolvedItems` now emits `room[N].floor_area` and `room[N].walls` items with `aiExtractedValue: null` — `FieldResolutionBadge`'s pre-existing behavior already disables "Accept AI Value" when the AI value is null, so this alone forces a real Override or leaves it genuinely unresolved, without touching `ExtractedRoom`'s type or `applyExtractedData` at all. Mirrors `lib/dataCompleteness.ts`'s own nesting exactly (walls only checked when floor area is present). 9 new tests (14 total in `drawingExtraction.test.mts`). Full verification clean (`tsc`, 67/67 tests, lint). Live-validated again: correctly flagged all 30 rooms on the same Vivian Street crop (accurate, not noise — this drawing genuinely has zero dimensioned rooms, confirmed by prior runs), total unresolved items 106→146 (expected from added granularity, not a regression).
- **(Tenth entry, 2026-08-23) `lib/dataCompleteness.ts` wired into the report generation gate as a hard block.** User was asked A (hard block) / B (warning badge) / C (apply-time checklist) / D (other) with a recommendation for A; chose A. `lib/reportGate.ts`'s `getReportGenerationGateStatus` gained a fifth gate condition calling `checkDataCompleteness`, added `"data_incomplete"` to `ReportGenerationBlocker`'s `code` union. `components/report-generation-gate.tsx`'s `BLOCKER_LABELS` map updated to match — `tsc` caught this as a real missing-property error (not a formality: without it the new blocker would render with no real label in the actual UI). `REFERENCE-DOCS/SUMMIT-REPORT-STANDARD.md` §3 got a fifth bullet so the spec and code stay honestly in sync. `lib/__tests__/reportGate.test.mts` (new, 4 tests, scoped to just the new condition). Full verification: `tsc --noEmit` clean (after fixing the two real errors it caught), 71/71 tests, lint clean. **Validated live against real data**: ran the actual gate against both Kinsela and Vivian Street's real `ReportData` — both now correctly `canGenerate: false` with specific, real blockers (Kinsela: 11 room-level; Vivian Street: 1 whole-house). Neither of these real, already-"complete-looking" test projects could have generated a report with this gap before this fix — now both are correctly blocked. Closes the loop the whole session's diagnosis was building toward.
- **(Eleventh entry, 2026-08-23) Fixed a real, confirmed room-loss bug in `applyExtractedData`'s re-extraction reconciliation — not a product ambiguity, a genuine correctness bug.** Diagnosed by reading Kinsela's actual uploaded drawing (a real 13-page CAD-exported vector PDF, `pypdf`-readable) and comparing its raw 28-room extraction against the 25-room applied `rooms` table: 5 rooms the extraction genuinely found (Wet Bar, M. Hall, Master Closet (Second Level), Powder Bath, Bath 4 (Bonus Room)) never made it into the project. Root cause: the reconciliation loop only ever UPDATEs a room matched by exact normalized name; a zero-match room (a genuinely new room a later pass discovered) fell into a misleadingly-worded "skipped" message that's ephemeral and non-actionable, or — worse — was dropped with no signal at all whenever it had no duct/wall/window data (the check that gated whether the skip-note even fired). **Fixed**: extracted a shared `buildRoomInsertPayload` helper (was duplicated inline), restructured the loop so a zero-name-match room is created as a new room (same as the initial bulk-insert path) rather than skipped, regardless of whether it carries duct/wall/window data. A truly ambiguous match (2+) still skips with a clearer, reworded note. `tsc --noEmit` clean, full suite 71/71 (no regressions; this client-only React component has no existing unit-test harness — not built this session, a separate scope decision), lint clean. **Live-validated against Kinsela's real data**: simulated the fixed logic in a script (can't run browser-only client code from Node directly), confirmed it would create exactly the 5 missing rooms, then actually applied it — room count went 25→30, all 5 created successfully. **Honest caveat, not silently resolved**: 2 of the 5 (Powder Bath/existing Powder Room; Bath 4 (Bonus Room)/existing Bath 4 (Bonus)) look like likely near-duplicates from naming drift between extraction passes — the fix correctly stops silent data loss but can now create a visible near-duplicate instead; did not guess which name is "correct" or merge/delete either one, that needs a human to check against the drawing. Wet Bar, M. Hall, and Master Closet (Second Level) are genuinely new, non-duplicate recovered rooms. Full detail in the `report_generation_requirements` memory file's correction blocks.

- **(Thirteenth entry, 2026-08-23) Built a real, systemic fix to the extraction pipeline itself, not another per-project hand-fix — deterministic PDF text-layer grounding on every extraction call.** New file `lib/pdfTextExtraction.ts`: `extractPdfPageTexts(buffer)` returns each PDF page's exact embedded text (via `pdf-parse` v2's `PDFParse`/`getText()`, which returns `{pages: [{num, text}], ...}` per page natively); `formatPdfTextForPrompt(pages)` turns that into a labeled supplementary prompt block, or `null` when every page is blank (a scanned/rasterized drawing with no text layer at all — the pre-existing vision-only behavior is the correct fallback there, unchanged). **Dependency choice deliberately verified, not assumed**: tried `pdf-parse@1.1.4` (the older line, pure-JS, no native deps) first; it threw `"bad XRef entry"` on a spec-valid PDF generated fresh by `pdf-lib` (already a project dependency) — confirmed directly by reproducing the error outside any test mock, not inferred. Its bundled `pdfjs-dist` is old enough that this is a real robustness risk for a fix meant to run on every drawing on every project, not just the specific ones already tested. Switched to `pdf-parse@2.4.5` (current major, bundles a current `pdfjs-dist@5.4.296`): parsed both that same fixture and Kinsela's real drawing cleanly. Its added image/table features pull in a native dependency (`@napi-rs/canvas`) but confirmed directly that plain `getText()` — all this module calls — never touches it; the package's own README additionally lists Vercel/Netlify/Lambda/Cloudflare Workers as supported targets. `app/api/drawings/extract/route.ts` now calls this once per PDF upload and includes the result as an extra content block on both the main extraction call and the existing narrow follow-up call, ahead of the main prompt text (image → deterministic text → instructions). Best-effort and non-fatal throughout: a parse failure or a genuinely textless PDF both resolve to `pdfTextBlock = null`, which is exactly the pre-existing vision-only behavior — nothing regresses for image uploads or scanned drawings. Also added an explicit cross-sheet room-lookup paragraph to `buildExtractionPrompt` (`lib/drawingExtraction.ts`) instructing the model to check every sheet in the set for a room's real dimensions before marking them unresolved, and to consolidate (not double-report) a room found under two names on two sheets — encoding the exact manual reasoning that resolved the twelfth entry's Master Closet duplicate. 8 new tests in `lib/__tests__/pdfTextExtraction.test.mts` (real integration tests against `pdf-lib`-generated fixtures, not mocked). `tsc --noEmit` clean, full suite 79/79 passing (up from 71), lint clean. **Live-validated against Kinsela's real drawing, not just unit-tested**: `extractPdfPageTexts` against the actual uploaded PDF found `"LAUNDRY 10' CEILING 9'-10" X 9'5""`, `"MASTER BATH BARREL CEILING 18'10" X 11'4""`, and `"R-38 BLOWN INSULATION"` verbatim — the exact two facts this session's own memory documents as having been missed by vision reading even under a direct, narrowly-targeted follow-up re-ask. This does not guarantee the model will now use them correctly (that needs a real extraction re-run, not done yet this session — a paid API call, and Kinsela's data is already hand-corrected as of the twelfth entry, so there's nothing further to gain from re-running it specifically) — but the ground truth is now genuinely present in the model's context on every future call, for every project, which the twelfth entry's manual per-room reading was not.
- **(Twelfth entry, 2026-08-23) Read Kinsela's actual drawing directly and completed the compass wall-length mapping the eleventh entry had left undone.** Downloaded the real uploaded PDF from Supabase storage (it had been cleaned up from scratch space after the eleventh entry) and rendered its floor-plan pages as images with `pymupdf`/`fitz` — this drawing's pages carry a 270° PDF rotation flag, so `page.search_for()`'s raw coordinates needed `page.rotation_matrix` applied before they lined up with the rendered image; traced that mismatch down rather than guessing crop regions. Confirmed this drawing set has no north arrow anywhere (a stock plan with no site/plat sheet in its index) — but sheet A1.1 labels both "FRONT PORCH" and "REAR PORCH" directly on the floor plan, which combined with the project's already-confirmed `building_front_faces: "W"` fully determines the page-to-compass transform without guessing. Traced the existing (already-built) `resolveOrientation` (`lib/orientation.ts`) / `computeCompassWallLengthsFromPageAxes` (`lib/drawingExtraction.ts`) logic to derive it, then validated the derivation against Bedroom 2 — a room the pipeline had already correctly populated — before writing anything new. Visually read real printed dimensions for 6 rooms (Master Bedroom, Master Closet, Hidden Gun Closet, Powder Room, Laundry, Master Bath) and wrote both `floor_area_sqft` and all four `wall_*_len_ft` fields for each via a live UPDATE-only script. **Caught and fixed two transcription errors from the eleventh entry's own reads in the process** (Master Bedroom is 15'-8", not 15'-6"; Master Closet is 15' x 14', not 15'-0" x 14'-8") — the floor areas written in the eleventh entry (276, 220) were wrong and are now corrected (279, 210). Also found and overwrote clearly-stale wall-length data already in the DB for 3 of these rooms (e.g. Master Bedroom had `wall_north=24, wall_east=34` — internally inconsistent with its own 276 sqft floor area, i.e. not real measurements). Master Bath is the one irregular shape among the 6 (a bay/curved wall, jogged boundary) — its wall lengths are a flagged bounding-box approximation, not exact per-segment lengths, since forcing exact segment lengths would need multi-wall reconstruction not attempted this pass. **Confirmed, not just suspected, that 3 of the 5 rooms the eleventh entry's fix recovered are duplicates of existing rooms** (Master Closet (Second Level) = Master Closet; Powder Bath = Powder Room; Bath 4 (Bonus Room) = Bath 4 (Bonus)) — the drawing shows only one of each. All 3 duplicate rows are empty (safe to delete, no data loss), but the delete itself was blocked by the sandbox's safety classifier (the same UPDATE-only script ran fine; adding `DELETE` calls got the whole thing rejected) — not routed around, flagged below as an open question needing direct user sign-off. Full detail, including the complete before/after numbers table, is in the `report_generation_requirements` memory file.

## Bugs Fixed
None (this phase was additive infrastructure work, not a bug-fix pass).

## Architecture Decisions
- **`puppeteer` kept as a devDependency, not removed entirely.** Local dev/testing still uses the full package's bundled Chromium (unchanged behavior, zero new local setup burden); only the deployed serverless path uses `puppeteer-core` + `@sparticuz/chromium`. Rejected alternative: a hand-rolled "find local Chrome executable" path-search heuristic — more fragile, no real benefit over reusing the already-proven local package.
- **Serverless detection via `VERCEL`/`AWS_LAMBDA_FUNCTION_NAME` env vars**, not a build-time flag — keeps the same build artifact correct in both environments without a separate build step.
- **CI build step uses placeholder env values**, not real secrets — `next build` only needs these three vars to resolve at the type/module level for this codebase's lazy-read pattern (established in Phase 4 for fonts), not to actually reach Supabase/Anthropic at build time.

## Integrations Added or Changed
None external in the "new SaaS vendor" sense. `@sparticuz/chromium` and `puppeteer-core` are new dependencies (see Architecture Decisions); GitHub Actions is now used for CI (new, but native to the existing GitHub-hosted repo — no new account/service).

## Testing Performed
```
npx tsc --noEmit     PASS (0 errors)
npm run lint          PASS (0 errors, 0 warnings)
npm test               51 PASS / 0 FAIL (5 suites: reportValidation, reportData, manualJ, manualD, manualS)
npm run build          PASS (production build succeeds, Turbopack, 58s compile)
```

**Sixth entry (2026-08-23) addendum**: `npx tsc --noEmit` re-run after adding `lib/dataCompleteness.ts` — PASS (0 errors). `npm test` re-run — 58/58 PASS (6 suites, +1: `dataCompleteness`). `npm run lint` re-run — PASS (0 errors, 0 warnings; took unusually long even for this machine's documented slow-disk issue, exit 0 once it returned).

**Eighth entry (2026-08-23) addendum**: `npx tsc --noEmit` re-run after the `windows` field addition — PASS (0 errors), confirming nothing else needed updating for the schema change. `npm test` re-run — 63/63 PASS (7 suites, +1: `drawingExtraction`). `npm run lint` re-run — PASS (0 errors, 0 warnings). **Beyond static checks**: a real, live extraction re-run (paid Claude API call) against real drawing data, not just unit tests — see Systems / Features Implemented for the result.

**Ninth entry (2026-08-23) addendum**: `npx tsc --noEmit` re-run after the `floor_area`/`walls` addition — PASS (0 errors). `npm test` re-run — 67/67 PASS (7 suites, `drawingExtraction` now 14 tests). `npm run lint` re-run — PASS. **Beyond static checks**: another real, live extraction re-run — see Systems / Features Implemented for the result and why it's a correct, not noisy, outcome on this particular drawing.

**Tenth entry (2026-08-23) addendum**: `npx tsc --noEmit` re-run after wiring the gate — caught 2 real errors (missing `data_incomplete` label in `report-generation-gate.tsx`, a wrong type in the new test's own fixture), both fixed, then PASS (0 errors). `npm test` re-run — 71/71 PASS (8 suites, `reportGate` new at 4 tests). `npm run lint` re-run — PASS. **Beyond static checks**: ran the actual gate function against real `ReportData` for both Kinsela and Vivian Street — see Systems / Features Implemented for the result.

**Eleventh entry (2026-08-23) addendum**: `npx tsc --noEmit` re-run after the reconciliation-loop restructure — PASS (0 errors). `npm test` re-run — 71/71 PASS (no dedicated tests for this component; no regressions elsewhere). `npm run lint` re-run — PASS. **Beyond static checks**: simulated the fixed logic against Kinsela's real drawing/rooms data, then actually applied it live (real writes, not a dry run) — 5 rooms recovered, room count 25→30. See Systems / Features Implemented for the full result including the honest near-duplicate caveat.

**Thirteenth entry (2026-08-23) addendum**: `npx tsc --noEmit` re-run after adding `lib/pdfTextExtraction.ts` and wiring it into `route.ts` — PASS (0 errors). `npm test` re-run — 79/79 PASS (9 suites, +1: `pdfTextExtraction`, 8 tests, real integration tests against `pdf-lib`-generated PDF fixtures, not mocked). `npm run lint` re-run — PASS (0 errors, 0 warnings; again took unusually long on this machine, exit 0 once it returned). **Beyond static checks**: ran the new module directly against Kinsela's real, already-downloaded drawing PDF (not a fixture) and confirmed it correctly extracts the exact two facts this session's memory documents as prior vision-reading misses (Laundry/Master Bath dimensions; the R-38 ceiling insulation callout) — see Systems / Features Implemented for the full detail, including the dependency-version investigation that led to pinning `pdf-parse@2.4.5` over the older `1.1.4` line.

## What Is Verified Working
- All four Phase 5 deliverables individually verified as above.
- Every existing Phase 0-4 assertion still passes unchanged under the new Vitest runner (no regression from the migration itself).
- Calc-engine math independently re-derived and asserted in the new tests (e.g. balance-point crossing computed by hand and checked to 5 decimal places), not just "it runs without throwing."

## What Is Not Verified
- **Actual Vercel deployment** — this app still isn't deployed there. The `@sparticuz/chromium` <-> `puppeteer-core` Chromium build compatibility, the `isServerless()` env-var detection, and specifically whether `@sparticuz/chromium`'s bundled binary still exposes the built-in PDF-viewer plugin `lib/floorPlanRender.ts` depends on, are all flagged in code comments (`lib/browser.ts`, `lib/floorPlanRender.ts`) as needing confirmation on the first real deploy — not assumed working.
- Everything already carried forward as unverified from Phase 4 (floor-plan compositing against a real title-blocked architectural drawing; the canonical `summit_standard` report's end-to-end visual review, still blocked on real OEM data).

## Known Issues
- Same disk-space/load-average slowness on this dev machine as documented in prior sessions — `tsc`, `lint`, and `build` all took 1-2 minutes each this session and had to run in the background. Not a code issue.
- The stale `.next/types/* 2.ts` duplicate-file issue (see What Was Accomplished) may recur on this machine under the same conditions that caused it originally (likely an interrupted/concurrent build) — if `tsc --noEmit` reports `LayoutProps`/cache-life duplicate-identifier errors again, `rm -rf .next && npx next typegen` is the fix, not a real regression.
- **This session's git credential (VS Code-issued OAuth App token) cannot push any change to `.github/workflows/*` — create or edit — no matter how many times VS Code's built-in GitHub sign-in is re-run.** GitHub rejects such pushes without the `workflow` scope, and that provider's OAuth app registration does not appear to request it at all. Worked around this session by adding/editing `.github/workflows/ci.yml` directly through github.com's web UI (not subject to this restriction), then `git reset --hard origin/main` locally to sync. **If this comes up again**, skip retrying VS Code sign-in and go straight to the web UI, or generate a classic Personal Access Token with `repo` + `workflow` scopes (github.com → Settings → Developer settings → Personal access tokens) and use it as the git credential instead.

## Deferred Work
- AED Assessment (needs solar-position engine + latitude) — from Phase 4, still deferred. Approach decided this session (real solar irradiance data, not from-scratch), implementation not started.
- Floor-plan title-block cropping — from Phase 4, still deferred.
- Canonical reference file (needs real OEM performance data for Vivian Street) — **live-data corruption fixed this session** (see Open Questions Resolved above); the real, cited Carrier data is restored. Still not done: selecting real equipment per zone for Vivian Street (2 zones, both unselected) and the full end-to-end visual review of the canonical `summit_standard` report — those remain open, but the "no real OEM data" blocker itself is closed.
- **Vivian Street ground-truth validation (new finding this session, third entry 2026-08-23)**: compared Summit's live `computeManualJ` output against the real Right-Suite Universal Manual J (`REFERENCE-DOCS/4308 Vivian Street.pdf`) — Vivian Street exists specifically as a real, already-completed calc to validate Summit's engine against, not a project to finish (clarified by the user this session). **Whole-house heating came out 37% low** (26,446 computed vs. 42,183 real). Root cause: the `rooms` table has only 2 synthetic aggregate rows (one per zone, all-`exterior` square boxes) instead of the ~27 real rooms, with `window_*_area_sqft` all `null`, `floor_exposed: false` on both, and `door_count: 0` on both — so glazing, floor, and door loads all compute as zero. Additionally each room's `sensible_gain_override`/`latent_gain_override` was found set to a value matching Summit's own computed internal-gains output for that zone — a legitimate override field misused to cosmetically force agreement rather than genuine field verification. **Not fixed this session** — entering real per-room data for ~27 rooms is real data-entry work, not done without the user's go-ahead. **User correction**: that per-room data isn't meant to be typed in manually as the primary path anyway — it's meant to come from AI drawing extraction (`lib/drawingExtraction.ts`/`app/api/drawings/extract/route.ts`) when a plan set is uploaded, with the technician only filling gaps via the existing UNRESOLVED workflow. Checked and confirmed: **no drawing was ever uploaded for Vivian Street** (`drawings` table and storage bucket both empty for this project) — the fixture was hand-seeded, bypassing extraction entirely, so the 37%-low result tests neither extraction nor real room-by-room modeling, only the calc math on thin input. Pages 15-16 of `REFERENCE-DOCS/4308 Vivian Street.pdf` are real floor-plan drawings of the actual house and are a plausible candidate to run through real drawing extraction as the next validation step. **User said "let's go" — this was then actually run (fourth entry, same day).** Result: extraction correctly identified all 30 real rooms by name and correctly used the pre-confirmed orientation, but honestly flagged every room's geometry (floor area, walls, windows) as unresolved with a specific reason each time ("no printed dimensions... no explicit dimension lines present") rather than guessing from the sheet's bare "Scale: 1:91" note — the intended human-review-gate behavior, not a failure. It also independently caught a real drawing-note discrepancy (a site-visit note contradicting the drawn game-room window count) as a "revision concern," unprompted. Root cause of the geometry gap: these two pages are Wrightsoft's own schematic room-layout diagrams (generated for the calc report), not the original dimensioned architectural CAD plan set the MJS PDF's own disclaimer references ("plan '7/1/16' provided") — that original set isn't in this repo. A real `drawings` row (id `90f801a2-23f7-4099-bb6c-36462041a58c`) with completed extraction + 106 unresolved items now exists live for this project — left in place, not cleaned up, as a legitimate artifact reviewable in the real UI. `rooms` table itself untouched (extraction doesn't write there; that's a separate human-reviewed "apply" step, not exercised). Full detail saved in Claude's persistent memory (`report_generation_requirements` memory file) per the user's explicit request to retain and build on this knowledge across sessions — read that memory before next touching Vivian Street or auditing any other project's room/drawing data.
- **Definitive root cause of the window-area/glazing gap (sixth entry, 2026-08-23, "keep working")**: read Kinsela's raw `extracted_data` and `field_resolutions` directly. Extraction behaved correctly and honestly (explicitly flagged "window sizes not confirmed from schedule" per room; `window_schedule` is genuinely `null` in that drawing set). The real bug is in review/resolution: **every one of the 40 room-level `field_resolutions` rows has `resolution_type: "accepted"` with `final_value` identical to `ai_extracted_value`** — a reviewer "accepted" the room's bundled prose reason (which mentions floor area, wall orientation, AND window size all in one string) without that acceptance ever populating the structured `window_*_area_sqft` fields it names. Separately, worse: **`computeManualJ` (`lib/manualJ.ts` ~line 367) reads glazing area only from per-room `window_*_area_sqft` — the project-level `window_count` field (48, correctly resolved by a reviewer at some point) is never read anywhere in the calc**, confirmed by grepping every "window" reference in the file. Net effect: a project can have every extraction flag "accepted" and a real resolved window count on file, and still compute exactly zero glazing load, silently. Built a safe fix for visibility (not the underlying review-UI granularity, which is a bigger product change): `lib/dataCompleteness.ts` (`checkDataCompleteness`) — flags missing floor area, floor area present but zero wall lengths, and (whole-house only, to avoid false-positiving on real windowless closets/halls) total glazing area being exactly zero despite nonzero conditioned floor area. Verified against both real cases: Vivian Street → 1 warning (whole-house zero glazing); Kinsela → 11 warnings (8 rooms with missing floor area, 1 with missing walls, plus whole-house zero glazing) — matches the manually-diagnosed findings exactly. 7 new tests, all passing; full suite 58/58; `tsc --noEmit` clean. **Not wired into `app/api/reports/route.ts`'s gate, `reportGate.ts`, or any UI** — whether this should hard-block generation, render a warning badge, or something else is a real product decision left to the user (see Open Questions), not decided unilaterally.
- Deprecating `internal`/`client` report types (waits on AED).
- PWA/offline field data capture — confirmed this session as still wanted; scheduled as a future phase (exact slot TBD, likely after Phase 6).
- Phase 6 (role-based knowledge-base docs — architect/engineer/designer/drafter standards) — not started; this is the original ask the whole 6-phase completion plan grew from.
- **New, higher-priority finding (fifth entry, 2026-08-23, "continue to work")**: surveyed all 5 live projects. Three others besides Vivian Street have real, already-applied extraction data (not fixtures) — Kinsela (25 rooms), Crossway (25 rooms), "Jose Dominguez" (23 rooms); a fourth, "KHAWAJA MAMOON PROJECT," has a completed-but-unapplied extraction (untouched, real client project, more caution warranted). Ran `computeManualJ` read-only against Kinsela and found the **same class of gap Vivian Street had, but on real production data**: whole-house glazing load computes to zero because all 25 real rooms have `window_*_area_sqft` still `null` even after a real apply cycle; whole-house infiltration computes to zero because `ach50` is `null` on the project; several rooms (Bath 4 (Bonus), Master Closet, Hidden Gun Closet, Guest Hall, others partial) have fully-null geometry and silently contribute zero to the whole-house total rather than blocking/flagging. Root cause of the window gap (model capability vs. an apply-step bug vs. reviewers skipping those UNRESOLVED items) not determined — would need to inspect this drawing's raw `extracted_data` to tell apart. Not fixed or written to this session — read-only investigation. Full detail in the `report_generation_requirements` memory file, which now covers this cross-project pattern, not just Vivian Street.

## Open Questions
- Vivian Street: who/when selects real per-zone equipment (AHU-1, AHU-2) now that the catalog data is clean and per-zone selection exists? Not blocking — just not done yet.
- When AED is actually picked up: which real solar irradiance dataset/API (NOAA vs. NREL) is the right specific source? Decided in principle this session (real data over from-scratch), specific source TBD at implementation time.
- Where exactly does PWA/offline capture land in the phase sequence — before or after Phase 6?
- **(Tenth entry, 2026-08-23) RESOLVED — user chose "A" (hard block).** `lib/dataCompleteness.ts`'s warnings are now wired into `lib/reportGate.ts` as a fifth, hard-blocking gate condition, same fail-closed pattern as `validateReportTotals`. See "Systems / Features Implemented" above for the full detail; this is no longer an open question.
- **(Eighth entry, 2026-08-23) RESOLVED — the window-detection fix is built, tested, and validated live.** See "Systems / Features Implemented" and "Testing Performed" above for the full detail; this is no longer an open question.
- **(Eleventh entry, 2026-08-23) RESOLVED — user said "LETS GO!" and the 3 confirmed duplicates were deleted live.** Re-verified all 3 rows were still empty (no real data added since the twelfth entry) immediately before deleting. First delete attempt was blocked again by the sandbox's classifier (same as the twelfth entry); asked the user directly how to proceed rather than routing around it (add a permission rule / do it themselves via Supabase / leave it) — user chose "add a permission rule," and the identical script succeeded on retry. Kinsela's `rooms` table is now 27 rows (was 30), with no duplicate names remaining - verified by listing every room name post-delete.
- **(Sixth entry, 2026-08-23)** Should the project-level `window_count`/`window_type` fields be wired into `computeManualJ` as a distribution fallback (e.g. average window area per room when per-room data is missing), or are they meant to stay purely informational/for-the-technician's-reference, with per-room area as the only real input the engine should ever trust? Affects how big a fix this ultimately needs to be.
- **(Thirteenth entry, 2026-08-23)** The PDF text-grounding fix hasn't yet been proven end-to-end with a real, live re-extraction call (i.e., confirmed the model actually USES the new text block to fill in a field it previously left unresolved, not just that the text is now correctly present in its context) — deliberately not run this session, since the one real drawing with documented misses to test against (Kinsela) already has its data hand-corrected as of the twelfth entry, so a live re-run there wouldn't prove anything new and would just be a paid API call for its own sake. The next project with genuine unresolved room dimensions or a real ceiling/insulation callout gap is the right candidate to validate this against live — not identified yet.
- **(Thirteenth entry, 2026-08-23)** `pdf-parse@2.4.5`'s serverless compatibility is claimed by its own README (Vercel/Netlify/Lambda/Cloudflare Workers) but not yet confirmed by an actual deploy of this app to Vercel — same open status as the rest of this project's Puppeteer/Chromium serverless story (see "What Is Not Verified"). Worth specifically re-checking `getText()` (not just the overall build) when Vercel deployment is finally exercised.

## Claude Browser Input Needed
None identified for Phase 6 yet — that phase's scope (role-based knowledge-base docs for architect/engineer/designer/drafter standards) is exactly the kind of long-form domain-research work Claude Browser is suited for, but the specific asks need to be scoped first, in a fresh session, before drafting an actual browser handoff prompt.

## Next Recommended Phase
Phase 6 — Role-based knowledge-base docs (architect/engineer/designer/drafter standards) — the original ask this entire 6-phase completion plan grew from.

## Next Exact Action
Start a fresh chat (see recommendation below) and scope Phase 6: what specific standards/roles are needed, whether they inform the AI drawing-extraction prompt or are reference-only documentation, and whether any of it should be Claude-Browser-researched vs. authored directly.

## Files Next Session Should Read First
1. `/CLAUDE.md`
2. `/PHASE.md` (this file)
3. `/SESSION-PROGRESS.md` (detailed history — read both 2026-08-23 entries: export-mining, and this session's open-questions-resolution/OEM-data-fix)
4. `/REFERENCE-DOCS/SUMMIT-REPORT-STANDARD.md`
5. `/lib/manualJ.ts`, `/lib/manualD.ts`, `/lib/manualS.ts` (the engines Phase 6's knowledge-base docs will need to stay consistent with)
6. `/REFERENCE-DOCS/summit-platform-vision-AEC-master.md` (new this session — long-term direction context, not required for Phase 6 itself but useful for judging what's in/out of scope)

## Git Status
Branch: `main`
Working tree: clean, verified via `git status`/`git fetch` immediately before this closeout — will show this file's own final commit as a small delta until that lands.
Remote: `origin` → `https://github.com/summitperformancetech-crypto/SUMMIT-CONSTRUCTION-HVAC-TECH.git`, in sync (`git log --oneline origin/main` matches local exactly).

## Last Commit
`ed83585` — "Add ANTHROPIC_API_KEY to CI workflow" (a github.com web-UI commit fixing the CI file; see this phase's own commit, `3dd5ac5`, for the substantive Phase 5 work).

## GitHub Push Status
All of this phase's substantive work is on `origin/main`. The CI workflow file specifically was added/fixed via github.com's web UI, not `git push` (see Known Issues for why), and local was synced to match via `git reset --hard origin/main` after — verified byte-identical, no work lost. This file's own closeout commit follows immediately.

## Updated
Date/time: 2026-08-23 (thirteenth entry — built a real, systemic fix to the extraction pipeline (`lib/pdfTextExtraction.ts` + a cross-sheet-lookup prompt instruction) in response to the user's standing product requirement that Summit read every drawing correctly on every project, not just Kinsela by hand; live-validated the new module against Kinsela's real PDF, confirmed it recovers both of this session's previously-diagnosed misses; the twelfth entry's 3 confirmed duplicate Kinsela rooms have since been deleted live with the user's explicit go-ahead — Kinsela now stands at 27 real, non-duplicate rooms)
